# 🧪 MikroTik Telegram Bot - Testing Strategy

هذه الوثيقة تمثل المرجع الرسمي لاستراتيجية الاختبار وضمان الجودة الخاصة ببوت إدارة MikroTik. تضمن هذه الاستراتيجية استقرار النظام وأمانه وتوافقه مع مختلف بيئات العمل.

## 1. Testing Pyramid
هرم الاختبار في هذا المشروع يركز على الكفاءة والسرعة:
- **Unit Tests (الاختبارات الأساسية - 70%):** اختبار الدوال المنفردة، وخوارزميات التشفير، وأدوات الحماية بدون الاعتماد على خدمات خارجية (باستخدام Mock API).
- **Integration Tests (اختبارات التكامل - 20%):** اختبار تدفقات Telegram الكاملة (Conversation Handlers) وتفاعلها مع طبقات الكاش وقواعد البيانات.
- **End-to-End Tests (الاختبارات النهائية - 10%):** اختبارات تحاكي دورة حياة المستخدم بالكامل، مع التركيز على السيناريوهات الحرجة باستخدام بيانات ومحاكاة شبه واقعية.

## 2. Test Categories
- **Regression Suite:** مجموعة الاختبارات الكاملة (حاليًا > 3,000 اختبار) التي تعمل مع كل تعديل لضمان عدم تعطل الميزات السابقة.
- **Security Tests:** فحوصات لمنع الوصول غير المصرح به (Admin Decorator) واختبار قوة تشفير كلمات المرور (Fernet).
- **Network Failure Tests:** محاكاة انقطاع الاتصال (Timeouts/Connection Errors) لضمان عدم انهيار البوت وتقديم ردود أخطاء ملائمة للمستخدم.
- **Stress & Performance Tests:** اختبار البوت تحت أحمال عالية (آلاف الطلبات المتزامنة) لضمان استقرار الاستهلاك وعدم وجود تسريب للذاكرة (Memory Leaks).

## 3. Test Coverage Matrix
| المكون البرمجي (Component) | نوع الاختبار | التغطية (Coverage) | الأدوات المستخدمة |
|---|---|---|---|
| Telegram Handlers | Integration | High | `pytest`, `PTB Mock` |
| RouterOS API (Core) | Unit / E2E | High | `librouteros`, Mock API |
| Security & Crypto | Unit | Critical | `pytest` |
| Database & Models | Integration | Medium-High | SQLite in-memory |
| Performance / Caching | Stress | Medium | `asyncio`, `scripts/stress_test.py` |

## 4. CI/CD Pipeline
يجب أن يمر أي كود جديد بالبوابات التالية (Quality Gates) قبل دمجه في الفرع الرئيسي `main`:
1. **Static Analysis:** فحص `ruff` لاكتشاف الأخطاء النحوية والأسماء غير المعرفة (E/F/W/I/UP/B).
2. **Type Checking:** فحص `pyright` (strict) لضمان التوافق التام لأنواع البيانات وعدم وجود تحذيرات.
3. **Handler Validation:** تشغيل `scripts/validate_handlers.py` للتأكد من ربط جميع معالجات الرسائل وقوائم الاتصال (Callbacks) بشكل صحيح.
4. **RouterOS Paths Validation:** تشغيل `scripts/validate_routeros_paths.py` لمنع مسارات User Manager المدمجة (Hardcoded) لضمان توافق v6/v7.
5. **Type Ignore Audit:** تشغيل `scripts/check_type_ignore.py` للتحقق من أن كل `# type: ignore` مبرر وموثّق.
6. **Test Suite:** تشغيل `pytest` مع تغطية لا تقل عن 80% (`--cov-fail-under=80`) والتأكد من نجاح 100% من الاختبارات.

> **ملاحظة:** وقت تشغيل المجموعة الكاملة ~20 دقيقة؛ أي مهلة CI أقل من 25 دقيقة ستقطع المجموعة أثناء التشغيل. اضبط مهلة خطوة الاختبار على 25 دقيقة على الأقل في GitHub Actions.

## 5. Required Tests Before Release
قبل أي إصدار جديد للإنتاج (Production Release)، يجب اجتياز الفحوصات التالية:
- نجاح الـ Regression Suite بالكامل (بدون أي أخطاء).
- فحص استقرار مسبح الاتصالات (Connection Pool) والكاش الذكي (TTL Cache).
- التأكد من قدرة `JobQueue` على أداء مهام النسخ الاحتياطي التلقائي والمراقبة (Watchdog) بنجاح.

## 6. Smoke Checklist
قائمة تحقق سريعة (آلية/يدوية) للتحقق من سلامة البوت بعد التشغيل الأولي:
- [ ] أمر `/start` يستجيب بسرعة ويعرض القائمة.
- [ ] أمر `/watchdog` يعمل ويُظهر حالة الراوترات المحفوظة بشكل سليم.
- [ ] البوت يتجاهل أوامر ورسائل المستخدمين غير المدرجين في `ADMIN_IDS`.
- [ ] ملف السجلات `logs/mikrotik-bot.log` يُسجل الأحداث بنجاح (مع تشفير وحجب كلمات المرور).

## 7. Manual Test Checklist
مهام يجب فحصها يدويًا من قبل المشرفين عند إجراء تغييرات جذرية على البنية التحتية:
- [ ] **إضافة مستخدم Hotspot:** التحقق من ظهور المستخدم في لوحة RouterOS وتوليد كارت PDF باللغة العربية.
- [ ] **النسخ الاحتياطي الكامل:** طلب `/backup` والتأكد من حفظ الملف في الخادم وإرساله عبر تيليجرام.
- [ ] **إضافة راوتر جديد:** إدخال بيانات راوتر باستخدام `/addrouter` واختبار نجاح الاتصال وتخزين كلمة المرور مشفرة.
- [ ] **إعادة تشغيل النظام:** التأكد من أن أوامر `JobQueue` المجدولة مسبقاً تُستعاد بنجاح بعد عملية الإيقاف الآمن (Graceful Shutdown).

## 8. Performance Benchmarks
تم اعتماد المعايير التالية كحد أدنى للكفاءة (بناءً على فحوصات الجودة):
- **100 طلب متزامن:** زمن استجابة < 3.5 ثانية (استهلاك المعالج 0%).
- **1000 طلب متزامن:** زمن استجابة < 6 ثوانٍ (اعتماد كامل على مسبح الاتصالات والكاش).
- **2000 طلب متزامن:** استهلاك الرام يجب ألا يتعدى 65 ميجابايت كحد أقصى.

## 9. RouterOS Compatibility Matrix
يدعم البوت العمل مع إصدارات RouterOS المختلفة مع تتبع تغيرات مسارات API الديناميكية:
| الميزة (Feature) | دعم RouterOS v6 | دعم RouterOS v7 | ملاحظات |
|---|---|---|---|
| **Hotspot Manager** | ✅ | ✅ | نفس المسارات (`/ip/hotspot/user`) |
| **User Manager** | ✅ (`/tool/user-manager`) | ✅ (`/user-manager`) | توجيه ذكي للمسار بناءً على الإصدار الذي تم التعرف عليه |
| **System Identity** | ✅ | ✅ | يعمل بشكل متطابق |
| **MNDP Discovery** | ✅ | ✅ | كشف عبر منافذ UDP 5678 |
| **Backup System** | ✅ | ✅ | متوافق تماماً مع النظامين |

## 10. Definition of Done (DoD)
يعتبر أي تحديث أو ميزة جديدة "مكتملة" فقط إذا تحققت الشروط التالية بالترتيب:
1. الميزة تُنفذ بوضوح وتؤدي الهدف المطلوب بدقة.
2. الكود مكتوب نظيفاً ولا يتسبب في تكرار (DRY).
3. تمت تغطية الميزة الجديدة باختبارات الوحدة والتكامل اللازمة.
4. اجتازت الميزة بنجاح بوابات الجودة (`ruff`, `pyright`, `validate_handlers.py`).
5. لا يوجد أي تسريب لبيانات حساسة (Logs Sanitization & Crypto).
6. تم تحديث الوثائق الداعمة (الـ Documentation) لتشمل التغيير.
