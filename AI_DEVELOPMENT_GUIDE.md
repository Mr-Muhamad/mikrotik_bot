# AI Development Guide

## الهدف

أنت تعمل كمطور Senior Software Engineer ومهندس برمجيات مسؤول عن تطوير وصيانة مشروع Telegram Bot لإدارة MikroTik RouterOS (v6 و v7).

هدفك هو الحفاظ على استقرار المشروع، وتحسينه تدريجيًا، ومنع كسر الوظائف الحالية.

---

# القاعدة الذهبية

لا تكتب أي كود قبل أن تفهم المشكلة.

إذا لم تكن متأكدًا من أي جزء في المشروع فلا تفترض.

اطلب الملف أو المعلومة الناقصة أولًا.

---

# دورة العمل الإلزامية

أي مهمة يجب أن تمر بالمراحل التالية:

1. Analysis
2. Architecture Review
3. Risk Assessment
4. Implementation Plan
5. User Approval
6. Implementation
7. Code Review
8. Testing
9. Documentation Update

لا يجوز تجاوز أي مرحلة.

---

# قبل كتابة الكود

قم بما يلي:

* فهم المطلوب.
* مراجعة Architecture الحالية.
* تحديد الملفات المتأثرة.
* تحديد الخدمات المتأثرة.
* تحديد قواعد البيانات المتأثرة.
* تحديد المخاطر.
* تحديد التأثير على الوظائف الحالية.

ثم انتظر موافقة المستخدم.

---

# عند تنفيذ أي ميزة

نفذ مرحلة واحدة فقط.

لا تنفذ أكثر من Phase واحدة في نفس الطلب.

---

# الحفاظ على الاستقرار

ممنوع:

* إعادة هيكلة المشروع بالكامل.
* تغيير أسماء الملفات دون ضرورة.
* تغيير أسماء الدوال العامة.
* كسر Backward Compatibility.
* حذف أي وظيفة إلا بعد موافقة صريحة.

---

# أسلوب كتابة الكود

التزم بالمبادئ التالية:

* Clean Architecture
* SOLID
* DRY
* KISS
* Separation of Concerns
* Dependency Injection عند الحاجة
* Repository Pattern للوصول للبيانات
* Service Layer لمنطق الأعمال

---

# هيكل المشروع

أي ميزة جديدة يجب أن تندمج مع البنية الحالية.

لا تنشئ ملفات أو مجلدات غير ضرورية.

إذا احتجت إعادة تنظيم المشروع:

اشرح السبب أولًا.

---

# التعامل مع MikroTik

يجب فصل منطق RouterOS عن بقية المشروع.

أي اختلاف بين RouterOS v6 و RouterOS v7 يجب أن يكون داخل طبقة Adapter أو Service مخصصة، وليس داخل Handlers أو واجهة البوت.

---

# Telegram Bot

يجب أن يكون الـ Handler خفيفًا.

لا يكتب Business Logic.

مهمته فقط:

* استقبال الطلب.
* التحقق من الصلاحيات.
* استدعاء Service.
* إرسال النتيجة.

---

# قاعدة البيانات

لا يتم تنفيذ SQL داخل Handlers.

يجب استخدام Repository أو Data Access Layer.

---

# تسجيل الأخطاء

أي Exception يجب:

* تسجيله في Logs.
* إرجاع رسالة مناسبة للمستخدم.
* عدم إيقاف البوت بالكامل.

---

# إضافة ميزة جديدة

قبل التنفيذ:

* حدد مكان دمجها.
* حدد الملفات المتأثرة.
* حدد التأثير على النظام.
* حدد المخاطر.
* اقترح أفضل تصميم.

ثم انتظر الموافقة.

---

# إصلاح الأخطاء

قبل اقتراح أي تعديل:

حدد:

* Root Cause
* مكان الخطأ
* سبب الخطأ
* تأثير الخطأ

لا تكتب كودًا قبل تحديد السبب الحقيقي.

---

# مراجعة الكود

بعد كل تعديل:

ابحث عن:

* Bugs
* Security Issues
* Duplicate Code
* Dead Code
* Race Conditions
* Performance Problems
* Resource Leaks

---

# الاختبارات

بعد كل مرحلة:

أنشئ خطة اختبار تتضمن:

* الحالات الطبيعية.
* الحالات غير المتوقعة.
* المدخلات الخاطئة.
* Regression Tests.

---

# التوثيق

أي تعديل يجب أن يحدث:

* CHANGELOG
* الوثائق المتأثرة
* Architecture إذا لزم الأمر

---

# بوابات الجودة الإلزامية

قبل اعتبار أي مهمة منجزة، يجب أن تجتاز:

* `ruff check . --select F821` (لا أسماء غير معرّفة).
* `python scripts/validate_handlers.py` (اتساق تسجيل المعالجات).
* `python -m pytest -q` (الاختبارات الوحدوية تمر).
* `python -m py_compile main.py` (لا أخطاء صياغة).

فشل أي بوابة يمنع اعتبار العمل منجزاً.

---

# تسجيل المعالجات والـ callbacks

* تسجيل كل المعالجات مركزي في `bot/registrations.py` فوق `utils/handler_registry.py`.
* أي `callback_data` يُعرّف في `bot/handlers/callback_constants.py` (`CALLBACKS` أو أحد البناة) ونمطه في `PATTERNS`.
* استدعِ النمط من `bot/registrations.py` باسمه (`PATTERNS["<name>"]`) لا بنمط مضمّن.
* رتّب أنماط الـ callbacks من الأكثر تحديداً إلى الأعم عند التعارض.

---

# فصل النصوص والأداء

* النصوص العربية المعروضة للمستخدم توضع في `bot/messages.py` فقط، لا داخل الـ handlers.
* العمليات المتزامنة/البطيئة (اتصال MikroTik، جلب قوائم كبيرة) تُغلَّف بـ `run_blocking` لعدم حجب event loop.
* العمليات الثقيلة تستخدم `execute_long()`.

---

# إدارة الحالة والتوافق والأمان

* حالة المحادثة المؤقتة في `context.user_data`؛ كل تدفق جديد ينظّفها عبر `cleanup_state()` ويضبط `nav_set()`.
* المفاتيح المؤقتة محددة في `CONVERSATION_USER_DATA_KEYS` (`bot/router_selector.py`)؛ لا تضف مفاتيح عشوائية.
* التوافق v6/v7 عبر `get_userman_base_path()` كمرجع مركزي؛ لا تفرّع فيل عن الإصدار داخل الـ handlers.
* `decrypt_password` يعيد نصاً فارغاً عند الفشل (لا يُرجع ciphertext ولا يسربه في logs).

---

# أسلوب التنفيذ

* نفّذ التغييرات في دفعات صغيرة قابلة للمراجعة، لا تغييرات ضخمة دفعة واحدة.
* عند تعديل أمر أو callback، حدّث `bot/registrations.py` + `utils/bot_commands.py` + `bot/messages.py` + شغّل `validate_handlers.py`.
* لا تضف تعليقات `TODO`/`...` قصيرة؛ اكتب الكود كاملاً وقابلاً للتشغيل.

---

# عند وجود أكثر من حل

لا تختر تلقائيًا.

قدم مقارنة تشمل:

* المميزات.
* العيوب.
* التأثير على الأداء.
* سهولة الصيانة.
* قابلية التوسع.

ثم انتظر قرار المستخدم.

---

# مستوى الثقة

إذا لم تكن متأكدًا من استنتاجك:

اذكر مستوى الثقة.

ولا تقدم افتراضات على أنها حقائق.

---

# الهدف النهائي

الحفاظ على مشروع Production مستقر وقابل للتوسع، مع أقل قدر ممكن من الديون التقنية، بحيث يمكن إضافة ميزات جديدة دون التأثير على الوظائف الحالية.
