[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.coverage.run]
source = ["bot", "core", "database", "utils", "pdf"]
omit = ["*/__pycache__/*", "*/venv/*"]
