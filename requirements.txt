# ── Runtime ──────────────────────────────────────────────────────────────────
python-telegram-bot[job-queue]>=21.0   # [job-queue] مطلوب لتشغيل JobQueue
librouteros>=3.3.0                      # MikroTik RouterOS API
python-dotenv>=1.0.0                    # تحميل ملف .env
cryptography>=41.0                      # تشفير كلمات المرور (Fernet)
psutil>=5.9.0                           # مراقبة أداء السيرفر (CPU/RAM)
matplotlib>=3.7.0                       # توليد الرسوم البيانية المصورة

# ── PDF & QR ─────────────────────────────────────────────────────────────────
reportlab>=4.0                          # توليد ملفات PDF
qrcode[pil]>=7.0                        # رموز QR — [pil] مطلوب لتوليد الصور
Pillow>=10.0                            # معالجة الصور (مطلوبة لـ qrcode[pil])

# ── Arabic text ───────────────────────────────────────────────────────────────
arabic-reshaper>=3.0.0                  # إعادة تشكيل الحروف العربية في PDF
python-bidi>=0.4.0                      # دعم النص ثنائي الاتجاه (RTL)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0
pytest-asyncio>=0.23.0                  # مطلوب لـ asyncio_mode = "auto" في pyproject.toml

# ── Development ───────────────────────────────────────────────────────────────
ruff>=0.3.0                             # فحص جودة الكود




# ─── Database & Migrations ──────────────────────────────────────────────
alembic>=1.13.1
sqlalchemy>=2.0.0
