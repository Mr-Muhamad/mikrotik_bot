# MikroTik Telegram Bot - Project Overview

## 1. الغرض
هذا المشروع عبارة عن بوت تليجرام مكتوب بلغة بايثون للتحكم براوترات مايكروتك (MikroTik RouterOS). يتيح النظام للمشرفين إدارة مستخدمي الهوتسبوت (Hotspot)، طباعة الكروت بصيغة PDF، أخذ نسخ احتياطية واستعادتها، مراقبة حالة الراوتر (Watchdog)، وإدارة نظام (User Manager) بكل سهولة عبر واجهة تليجرام.

## 2. الهيكلية المعمارية
- **الواجهة:** تعتمد على مكتبة `python-telegram-bot` (الإصدار 21+) باستخدام `ConversationHandler` متقدم.
- **التواصل مع الراوتر:** يتم عبر بروتوكول MikroTik API باستخدام مكتبة `librouteros` عبر منفذ `8728` (أو المخصص).
- **قاعدة البيانات:** يستخدم المشروع SQLite مُدعماً بنظام تهجير البيانات `Alembic`، حيث تُحفظ الروترات المحفوظة وتفضيلات الجلسات وتُشفر الكلمات السرية بـ `Fernet`.
- **هيكلة الملفات:**
  - `bot/handlers`: معالجات الأوامر والنصوص.
  - `bot/messages.py`: مركز تخزين كافة رسائل البوت (دعم اللغة العربية).
  - `core/`: طبقة الخدمات (إدارة الروترات، النسخ الاحتياطي، المراقبة).
  - `database/`: المخططات وعمليات الـ CRUD.
  - `utils/`: أدوات مساعدة للحماية والتحقق وتنسيق البيانات.

## 3. التعديلات الجوهرية الأخيرة
- اعتماد نماذج البيانات (Dataclasses) عبر `session_models.py` لحفظ حالة المحادثات بشكل آمن والتخلص من بدائية القواميس (Primitive Obsession).
- تطبيق ترقيم الصفحات (Pagination) في السيرفر والذاكرة لمنع امتلاء الشاشة بالقوائم الطويلة.
- مركزة الرسائل النصية لتجنب تكرار النصوص داخل المعالجات (Scattered Magic Strings).
- كسر الاستيرادات الدائرية (Circular Imports) عبر نقل دوال الـ Helpers لملفات محايدة `tg_helpers.py`.
- نقل إدارة قاعدة البيانات وتحديث المخططات إلى `Alembic` للترقيات الآمنة.

## 4. مسار التطوير المستقبلي
- إضافة دعم اللغات المتعددة بسهولة بفضل مركزة الرسائل.
- تحسين واجهات PDF لدعم ثيمات مختلفة.
- تعزيز مراقبة الروترات بإرسال رسوم بيانية عن الاستهلاك (`/metrics` المتقدمة).
